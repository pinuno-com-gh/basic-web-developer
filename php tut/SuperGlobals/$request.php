<?php 
/*Request is a superglobal variable which is used to 
 * collect the data after submitting an HTML form
 * $_REQUEST is not used most because $_post and $_GET 
 * does the same
 */

echo "the example for $_SERVERserver is PHP embeded in Html, so did not do that example"
    ?>